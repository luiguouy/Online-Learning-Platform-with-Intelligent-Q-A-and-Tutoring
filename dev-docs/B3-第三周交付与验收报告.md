# 成员 B 第三周交付与验收报告（B3.1 ~ B3.3）

- **验收分支/基线**：`feature/B-week3-delivery`，基于 `dev@f381388`（已含 A 的 security hardening、SSE 迁到矩阵冻结路径、M2 解析上限，以及 C/D 前端）
- **验收时间**：2026-09-20
- **交付人**：成员 B（后端业务与数据架构）
- **验收人**：成员 A（待 review）
- **对应计划**：`THREE_WEEK_PLAN.md` 5.2 节（Day 16~19），B3.1~B3.3 全部完成 + Gate 3 就绪

## 一、交付范围与结论

第三周任务 **B3.1 ~ B3.3 全部完成并通过端到端验收**，Gate 3（Day 19 代码冻结）前置条件就绪。

| # | 任务 | 验收标准 | 结果 |
| :--- | :--- | :--- | :--- |
| B3.1 | 边界与异常测试（空输入 / 超长 / 超大文件） | 均返回友好错误，不 500 | ✅ PASS（HTTP 层 46 项断言 PASS / 0 FAIL） |
| B3.2 | 修 P0 / P1 缺陷 | 缺陷清单清零 | ✅ PASS（5 类边界缺陷已修 + 1 项 P1 种子数据缺陷已修） |
| B3.3 | 种子数据复核 + 数据库脚本封板 | 一键建库建表可复现 | ✅ PASS（DROP 后重建可复现 + 幂等性验证通过） |

**本周代码改动 4 个文件**（相对 `dev@f381388`）：

| 文件 | 改动 | 归属 |
| :--- | :--- | :--- |
| `common/GlobalExceptionHandler.java` | 新增 3 个异常兜底 handler（参数类型 / 请求体解析 / 路径不存在） | B3.1 |
| `controller/SseChatController.java` | 空提问 / 超长提问前置校验 + `event:error` 下发 | B3.1 |
| `controller/TeacherDocumentController.java` | 文件名长度前置校验 + `transferTo` 异常兜底 | B3.1 |
| `resources/db/data.sql` | 删除 `course_document` PENDING 种子行 + 原因说明 | B3.2 / B3.3 |

另修正 `application.yml` 的 `max-http-request-header-size` 与两处过期注释（详见第四节）。

## 二、验收环境与方法

| 项 | 说明 |
| :--- | :--- |
| 应用 | `java -jar` 打包运行，Tomcat 8080，启动耗时 5.8s，JDK 17 |
| 数据库 | 本机 MySQL 8 真实库 `smart_qa`，**验收前 `DROP DATABASE` 后由 `schema.sql` + `data.sql` 重建** |
| 向量库 | 本机未起 Chroma，走 `InMemoryEmbeddingStore` 降级（同实例内上传后可检索命中） |
| 大模型 | **本地 OpenAI 兼容 Mock 端点**（`127.0.0.1:9999/v1`，`AI_BASE_URL` 注入），仅替换"生成应答文本"一步 |
| HTTP 层验收 | `.workbuddy/tools/week3_boundary_test.py`（B3.1，46 项）+ `week2_full_accept.py`（B2 回归，35 项） |
| 集成测试 | `mvn test`（真实 MySQL + `@MockBean` 假流式模型），8 项 |

## 三、逐条验收证据

### B3.1 边界与异常测试

**缺陷清单（修复前 → 修复后）**：B3.1 实测暴露 **5 类真实缺陷**，全部会在旧代码下返回 HTTP 500，均已修复为友好业务码。

| # | 触发条件 | 修复前 | 修复后 | 修复位置 |
| :--- | :--- | :--- | :--- | :--- |
| 1 | 路径/查询参数类型不匹配（`courseId=abc`） | 500 | `400 参数格式不正确：courseId` | `GlobalExceptionHandler` |
| 2 | 请求体 JSON 字段类型错（`status:"x"`） | 500 | `400 请求体格式不正确，请检查字段类型` | `GlobalExceptionHandler` |
| 3 | 文件名超长（299 字符 > 200 上限） | 500（`FileNotFoundException`） | `400 文件名过长（最多 200 个字符），请重命名后重试` | `TeacherDocumentController` |
| 4 | **SSE 空/纯空白提问** | **HTTP 500 空体** | `event:error {"errorCode":4000,"message":"提问内容不能为空"}` | `SseChatController` |
| 5 | 请求路径不存在（拼错接口 / 静态资源） | 500 | `404 请求的接口或资源不存在：/xxx` | `GlobalExceptionHandler` |

> **缺陷 4 的根因值得单独说明**：`/api/qa/chat/stream` 声明 `produces=text/event-stream`，而 `@RestControllerAdvice` 返回的是 JSON `Result`。前端 `EventSource` 发送 `Accept: text/event-stream`，Spring 内容协商找不到可写介质 → `HttpMediaTypeNotAcceptableException` → Tomcat 兜底 **HTTP 500 空体**，前端只能看到"连接失败"、没有任何原因。正确做法是**不再走异常体系**，直接在 Controller 里通过 `emitter.send(SseEmitter.event().name("error")...)` 下发 `event:error` 事件——本次已改用该方案。

**独立复核（不依赖验收脚本，直接读原始响应）**：
```
【参数类型】GET /api/qa/sessions?courseId=abc              → HTTP=200 code=400 "参数格式不正确：courseId"
【参数类型】GET /api/teacher/qa/records?courseId=abc&…      → HTTP=200 code=400 "参数格式不正确：courseId"
【请求体】  POST /api/qa/records/1/feedback status="x"      → HTTP=200 code=400 "请求体格式不正确，请检查字段类型"
【路径】    GET /api/does-not-exist                        → HTTP=200 code=404 "请求的接口或资源不存在：/api/does-not-exist"
【路径】    GET /robots.txt                                → HTTP=200 code=404 "请求的接口或资源不存在：/robots.txt"
【文件名】  upload 299 字符文件名                           → HTTP=200 code=400 "文件名过长（最多 200 个字符）…"
【大文件】  upload 51MB                                     → HTTP=200 code=400 "文件超过 50MB 上限，请压缩后重试"
【空提问】  question=""                                     → HTTP=200 events=[event:error] {"errorCode":4000,"message":"提问内容不能为空"}
【空白提问】question="   " / "\t\n  \r\n"                   → HTTP=200 events=[event:error] {"errorCode":4000,...}
```
全部为 **HTTP 200 + 业务码**，符合统一契约；无一处 500。

**提问长度三区间边界语义（二分实测）**：

| 字数 | query 字节 | 结果 |
| ---: | ---: | :--- |
| 1599 / 1600 | 14400 / 14409 | ✅ 放行出流（`references → message → done`） |
| 1601 / 1700 / 1790 / 1800 / 1810 | 14418 ~ 16299 | ✅ 业务拦截 `event:error {errorCode:4000}` |
| 1850 | 16659 | ⚠️ 协议层 HTTP 431（请求行超限，**进不到 Controller**） |

- 业务上限 `MAX_QUESTION_LENGTH = 1600` 含端点：1600 放行、1601 拦截。
- 协议层实测上限约 **16370 字节**，与业务上限之间仅约 40 字余量，因此 **1601 字以上的拦截必须发生在业务层**（否则退化为 431 无信息断连）。
- ⚠️ **给成员 C 的提示**：前端提问框需加 `maxlength`（建议 1600）并在超限时提示，不要依赖后端 431——431 是协议层拒绝，前端 `EventSource` 拿不到任何可读原因。

**B3.1 汇总**：`PASS 46 / FAIL 0`。

### B3.2 修 P0 / P1 缺陷

对 `dev@f381388` 基线做对抗式评估，逐项回到数据库原始行与接口实时返回核对，结论如下：

| 项 | 危害评估 | 处置 |
| :--- | :--- | :--- |
| SSE 空提问 500 空体 | **P1**（用户可轻易触发，前端完全无提示） | ✅ 本周修复（见 B3.1 缺陷 4） |
| 参数/请求体/路径异常 500 | **P1**（拼错路径即 500，前端难排查） | ✅ 本周修复（见 B3.1 缺陷 1/2/5） |
| `data.sql` 预置 `course_document` PENDING 种子行 | **P1**（前端轮询永不停止，演示被当 Bug） | ✅ 本周修复（见 B3.3） |
| `filePath` 暴露磁盘绝对路径 | P2（信息泄露，无直接可利用性） | ✅ 已由 A 的 `2b9a78c` 加 `@JsonIgnore` 修复 |
| `errorMsg` 回传服务器路径 | P2 | ⏳ 技术债，演示不阻塞 |
| `isDeleted` 字段下发到响应 | P2（无安全影响） | ⏳ 保留（便于前端排查逻辑删除） |

**数据库原始行核对**（API 已不下发 `filePath`，故直接查表）：
```
course_document id=11 → file_path = C:\Users\34677/smartqa/uploads/1/1789912786774_第3章_内存管理_验收.md（绝对路径已落库）
僵尸 PENDING 计数     → 0   ← B3.3 删除种子行后确认
6 张表 is_deleted 列  → sys_user / course / course_document / qa_session / qa_record / course_knowledge_point 全部存在
```
> 附：落库路径形如 `C:\Users\34677` + `/smartqa/uploads/...`，**分隔符混用**。经 Java `File` 实测（`exists()=true`、`isAbsolute()=true`、`getCanonicalPath()` 正确归一），Windows 上两种分隔符均被接受，**无功能影响**，仅观感问题，不修。

**B3.2 结论**：P0 缺陷 0 项、P1 缺陷 3 项全部清零；剩余 P2 项均为已评估的技术债，不阻塞 Day 19 冻结。

### B3.3 种子数据复核 + 数据库脚本封板

**一键重建可复现**（`DROP DATABASE` 后从零重建）：
```
步骤1  DROP DATABASE smart_qa           → [OK]
步骤2  schema.sql                       → CREATE DATABASE + 6 张 CREATE TABLE IF NOT EXISTS，全部 [OK]
步骤3  data.sql                         → sys_user 3 / course 3 / course_knowledge_point 4 / qa_session 1 / qa_record 1，全部 [OK]
步骤4  核对                              → 6 张表齐全；course_document = 0 行（★ 不再预置课件）
```

**幂等性验证**（两脚本各再执行一次）：
```
schema.sql 第二次执行 → [ERR] 数 = 0
data.sql   第二次执行 → [ERR] 数 = 0
行数对比              → sys_user 3 / course 3 / course_document 0 / qa_session 1 / qa_record 1 / course_knowledge_point 4  与首次完全一致（无重复插入）
```
两脚本分别使用 `CREATE TABLE IF NOT EXISTS` 与 `ON DUPLICATE KEY UPDATE`，**可重复执行**，满足"一键建库建表可复现"。

**`course_document` 种子行的删除理由**（已写入 `data.sql` 注释）：课件记录必须与"磁盘文件 + 向量库切片"三者一致才有意义。原种子行只有数据库记录、没有真实文件，导致三个真实问题：① 教师端课件列表首屏即出现 PENDING 条目，前端 3s 轮询永不停止；② 学生提问检索不到其内容，易被误判为 RAG 失效；③ 演示时容易被当成 Bug。**正确做法**：由教师端走真实上传流程，落盘 + 落库 + 切块三步自然一致，状态 `PARSING → CHUNKED/FAILED` 有明确终点。

## 四、随本次修正的两处"回头账"（重要）

验收中发现两周前的一处**误判**，本次一并纠正：

1. **`server.max-http-request-header-size` 确实会抬高请求行上限**。上一轮验收时曾判定"该项对请求行无效"并从 `application.yml` 删除——**该结论是错的**。二分实测：

| 配置 | 实测请求行上限 |
| :--- | :--- |
| 不配（Tomcat 默认） | ≈ **8056 字节** |
| `max-http-request-header-size: 64KB` | ≈ **16370 字节** |

   由于 `dev` 基线上该项缺失，rebase 后请求行上限退回 8056 字节，使 1600 字提问（14400 字节）触发 `IllegalArgumentException: Request header is too large` → HTTP 400，**造成 4 个测试用例回归失败**。本次已**重新加回 `64KB`** 并在配置注释与 `MAX_QUESTION_LENGTH` Javadoc 中记录实测数据与"两者配套、不可单独改"的约束。

2. `week2_full_accept.py` 中"落库 filePath 为绝对路径"的断言已过期：A 加 `@JsonIgnore` 后该字段**不再出现在响应里**。已把断言改成"响应不含 `filePath` 键"（这才是修复后的正确契约），字段落库仍由 B3.3 建库脚本与上传链路覆盖。

## 五、集成测试与 B2 回归

```
# 集成测试（mvn test，真实 MySQL + @MockBean）
[INFO] Tests run: 3, Failures: 0, Errors: 0 -- RagIsolationTest          （A，courseId 检索隔离）
[INFO] Tests run: 5, Failures: 0, Errors: 0 -- SseWiringIntegrationTest  （3 主流程/边界 + 1 新增边界值）
[INFO] Tests run: 8, Failures: 0, Errors: 0
[INFO] BUILD SUCCESS
```
`SseWiringIntegrationTest` 本次**重写了两条过期用例**（旧断言期望"超长提问正常出流"，与新增的业务上限冲突）：
- `oversizedQuestion`：改为断言"1800 字 → `event:error {errorCode:4000}`、不落库、不进入推流"。
- 新增 `questionAtUpperBound`：断言"恰好 1600 字放行出流且 `question` 不截断"，锁定"上限含端点"语义，防止日后被静默收紧。

**B2 全量回归**（`week2_full_accept.py`，全新库）：`PASS 35 / FAIL 0`。

## 六、已知限制与遗留

1. **本机无 `AI_API_KEY`**：真实大模型应答未端到端验证（用本地 Mock 端点替代）。演示前需配真实 Key。
2. **Chroma 未起**：走 `InMemoryEmbeddingStore`，**重启后向量丢失但 `parse_status` 仍为 `CHUNKED`**——最易被误判为"检索 Bug"，演示前必须起 Chroma。
3. **协议层 431 不可挽回**：提问 > 约 1810 字时请求行超限，请求进不到 Controller。**必须由前端 `maxlength` 拦截**（已提示成员 C）。
4. **`feedback` 无"取消评价"语义**：接口只接受 ±1，复位需 SQL。验收脚本已内置清理提示。
5. **技术债（不阻塞冻结）**：`errorMsg` 回传服务器路径、`isDeleted` 下发。

## 七、复现方式

```bash
# 1. 重建数据库（一键）
java DbRunner "jdbc:mysql://127.0.0.1:3306/…" root 123456 schema.sql data.sql
# 2. 起本地 Mock 大模型
python .workbuddy/tools/mock_llm.py 9999
# 3. 打包并启动（注意：宿主可能注入 SERVER__PORT，需 unset）
unset SERVER__PORT
AI_BASE_URL=http://127.0.0.1:9999/v1 java -jar backend/target/smart-qa-platform-1.0.0-SNAPSHOT.jar --server.port=8080
# 4. 验收
python .workbuddy/tools/week3_boundary_test.py   # B3.1，46 项
python .workbuddy/tools/week2_full_accept.py     # B2 回归，35 项
mvn test                                          # 集成测试，8 项
```

> 脚本放在 `.workbuddy/tools/`（已被 `.gitignore` 忽略），不随本 PR 入库。

## 附录：交付前对抗式复核记录

本轮复核坚持 **claim → evidence → reproduce**：不采信任何"之前测过"的结论，全部回到数据库原始行 / 接口实时返回 / Java API 实测重新验证。复核中推翻了 2 处此前结论（`max-http-request-header-size` 无效论、filePath 断言），并确认 5 类缺陷修复、B3.3 一键重建与幂等性均成立。
