# 术语表与零基础 FAQ（给零编程经验组员）

> **本文件是给"看不懂技术黑话"的组员准备的**。遇到听不懂的词先来这里查，再去问人。
> 配套文件：`COLLABORATION_WORKFLOW.md`（流程）、`DEV_SPECIFICATION.md`（代码规范）。

---

## 一、 术语表

### 1.1 AI 与 RAG 相关（成员 A 相关，但全员都要懂）

| 术语 | 一句话解释 | 类比 |
| :--- | :--- | :--- |
| **RAG** | 先从你的课件里找资料，再让大模型照着资料回答 | 开卷考试，而不是闭卷瞎编 |
| **大模型 / LLM** | 通义千问、DeepSeek 这类能生成文字的 AI 程序 | 一个很会说话但没有课本的学生 |
| **Embedding（向量化）** | 把一段文字变成一串数字，意思相近的文字数字也相近 | 给每段话发一个"语义指纹" |
| **向量库 / Chroma** | 专门存放和查找这些"语义指纹"的数据库 | 按相似度检索的图书馆索引 |
| **切块 / Chunk** | 把一篇长课件切成 400 字左右的小段 | 把书拆成卡片，方便精确查找 |
| **Top-K** | 返回最相似的 K 个片段（本项目默认 K=3~5） | 找最像的 3 张卡片 |
| **相似度 / score** | 问题与片段的接近程度，0~1，越高越相关 | 匹配度打分 |
| **幻觉** | 大模型编造不存在的事实 | 不会做题却硬编答案 |
| **Prompt** | 你给大模型的指令与背景资料 | 给学生的"答题要求+参考资料" |
| **Token** | 大模型计费与长度单位，中文字约 1~2 个 token | 按字数收费的"字数" |
| **SSE** | 服务器一点一点往前端推送文字的技术 | 打字机，一个字一个字往外冒 |
| **流式输出** | 答案边生成边显示，不用等全部生成完 | 看直播，而不是等录播 |

### 1.2 后端相关（成员 B 相关）

| 术语 | 一句话解释 |
| :--- | :--- |
| **Spring Boot** | 写后端接口的 Java 框架，本项目的技术底座 |
| **Controller / Service / Mapper** | 三层分工：接请求 / 处理业务 / 读写数据库 |
| **MyBatis-Plus** | 简化数据库操作的工具，省掉手写 SQL |
| **MySQL / DDL** | 数据库 / 建表的 SQL 脚本 |
| **CRUD** | 增删改查四种最基础的数据操作 |
| **Sa-Token** | 登录鉴权框架，管"你是谁、你有什么权限" |
| **BCrypt** | 密码加密存储方式，数据库里看不到明文密码 |
| **接口 / API** | 前后端约定的"调用函数"，有固定地址和参数 |
| **Knife4j** | 自动生成接口文档的网页，后端写完前端照着调 |
| **线程池** | 管理"干活的人手"的机制，防止人手被占满导致卡死 |

### 1.3 前端相关（成员 C/D 相关）

| 术语 | 一句话解释 |
| :--- | :--- |
| **Vue 3** | 写网页界面的框架 |
| **Vite** | 前端项目的启动与打包工具 |
| **Element Plus** | 现成的按钮、表格、弹窗组件库，不用自己写样式 |
| **Pinia** | 全局状态管理，比如"当前选中的课程" |
| **TypeScript / `.ts`** | 带类型检查的 JavaScript，能在写代码时就发现错误 |
| **Markdown** | 用符号表示格式的文本语法（`#` 是标题、`**` 是加粗） |
| **Axios** | 前端发请求的工具 |

### 1.4 协作与工程相关（全员必懂）

| 术语 | 一句话解释 |
| :--- | :--- |
| **Git / 仓库（Repo）** | 代码的版本管理系统与存放地（GitHub） |
| **commit** | 一次保存点，附一句话说明改了什么 |
| **分支（branch）** | 独立的开发线，互不干扰；做完再合并回主线 |
| **PR（Pull Request）** | "请把我的分支合并进去"的申请，附带评审 |
| **Review** | 别人检查你的代码，通过才能合并 |
| **merge / 合并** | 把分支的改动并入主线 |
| **冲突（conflict）** | 两人改了同一处代码，需要人工选择保留哪个 |
| **CI** | 自动编译检查的机器人，代码一提交就自动跑 |
| **Issue** | GitHub 上的任务单/问题单 |
| **.env / application-local.yml** | 存放密钥和本地配置的文件，**绝不入库** |
| **localStorage** | 浏览器本地存储，本项目用它存登录 Token（键名 `satoken`） |

---

## 二、 零基础 FAQ

### Q1：我完全不会写代码，怎么完成开发任务？

靠 AI Agent。你的角色不是"写代码的人"，而是**"提需求的人和验收的人"**：

1. 按 `README.md` 第二节的模板，把 3 份文档丢给你的 AI 工具
2. 让它先列出要创建/修改的文件清单给你确认（防止它乱来）
3. 让它一次只做一件小事（比如"只实现登录接口"）
4. 让它跑编译命令，**把成功的输出贴给你**
5. 跑不通就继续让它修，不要自己改代码，也不要跳过

**判断 AI 有没有在糊弄你的三个信号**：只说"已完成"但不贴运行结果；改了很多无关文件；编译报错就说"换个写法试试"但不解释原因。

### Q2：AI 写的代码报错了，我该怎么办？

按顺序做，**不要一上来就让它重头写**：

1. 把**完整报错信息**复制给它（从 `ERROR` 或 `Caused by` 那一行开始，往上 20 行）
2. 让它先解释"为什么错"，再让它改——**说不出原因的修复往往只是掩盖问题**
3. 改完必须重新编译验证
4. 连续 3 次修不好，换思路：让它把这一步拆得更小，或者回滚到上一个能跑的版本

### Q3：我不知道该问谁？

| 你的问题类型 | 找谁 |
| :--- | :--- |
| 接口字段对不上、返回格式不对 | 看 `DEV_SPECIFICATION.md` 4.2；还有疑问找接口提供方（见分工文档接口矩阵） |
| 数据库表结构、登录鉴权 | 成员 B |
| 大模型、向量检索、切块、SSE | 成员 A |
| 流程怎么走、要不要改契约、有分歧 | 成员 A（组长）拍板 |
| 我不知道今天该做什么 | 看 `TEAM_WORK_DIVISION.md` 里你那一周的任务清单 |

**问人之前先做两件事**：查本文件的报错速查表；把你已经试过的方法说清楚。这样别人 30 秒就能帮你，而不是花 10 分钟问你背景。

### Q4：我进度落后了怎么办？

**立刻说，不要藏着。** 流程见 `COLLABORATION_WORKFLOW.md` 7.3：阻塞超 4 小时群里 @ 组长，超 8 小时开语音会。
零编程经验下卡住，90% 的情况不是你不够努力，而是**任务拆得不够小**——让组长帮你切成"一次只做一件事"。

### Q5：AI 让我装一堆东西 / 改全局配置，我该照做吗？

**先问，再装。** 凡是涉及：安装全局软件、修改系统环境变量、删除文件、重装依赖、格式化代码库——一律先在群里确认。
唯一例外：`npm install`（前端装依赖）和 `mvn clean compile`（后端编译）是日常工作，可以直接做。

### Q6：我能直接改别人写的代码吗？

**不能。** 走 PR 流程。哪怕是改一个错别字，也要提 PR 让对方知道——这是企业级协作最基本的尊重与可追溯性。

### Q7：什么是"在我电脑上能跑"问题？为什么要 CI？

同一份代码在 A 的电脑上跑得通、在 B 的电脑上报错，通常因为：依赖版本不同、本地配置不同、有人提交了不完整的代码。
CI 就是**一个所有人都一样的第三方电脑**，每次提交都自动编译一遍，红灯就说明这份代码不通用。**红灯不过夜**。

---

## 三、 常见报错速查表

| 报错 / 现象 | 最可能的原因 | 处理 |
| :--- | :--- | :--- |
| `401 Unauthorized` / 未登录 | Token 没带或过期 | 检查请求头 `Authorization: Bearer <token>`；重新登录 |
| `403 Forbidden` | 角色不对，或没实现 `StpInterfaceImpl` | 见 `MEMBER_B_DEV_GUIDE.md` 3.3；确认账号角色是 TEACHER/STUDENT |
| `404 Not Found` | 接口路径或请求方式写错 | 对照 Knife4j 检查路径与 GET/POST |
| `500` + `NullPointerException` | 某个对象没取到就调用 | 让 AI 定位空指针行，加判空 |
| CORS 跨域报错（浏览器控制台） | 后端没配跨域，或 Vite 代理没生效 | 检查 `SaTokenConfigure` 的 CORS 配置与 `vite.config.ts` 代理 |
| SSE 一直 loading，不吐字 | 后端阻塞；或用了默认线程池被占满 | 确认注入了 `sseExecutor`；检查大模型 Key 是否有效 |
| SSE 收到乱码/解析失败 | 前端按裸文本解析，但后端发的是 JSON | `message` 事件要 `JSON.parse(data).delta`（见 C 指南） |
| `NoSuchBeanDefinition: sseExecutor` | 线程池 Bean 没配置 | 见 `AGENT_INSTRUCTIONS.md` 1.3 |
| Chroma 连不上 / `Connection refused` | Docker 容器没启动 | `docker ps` 检查，`docker restart chroma` |
| 检索结果总是空 / 不相关 | 课件还没切块完成；或 `courseId` 过滤写错 | 确认课件状态是 `CHUNKED`；检查 `IsEqualTo("courseId", ...)` |
| `mvn compile` 失败：依赖下载不了 | 网络或 Maven 镜像问题 | 检查 `settings.xml` 镜像源；让 AI 换阿里云镜像 |
| `npm install` 卡住/失败 | 网络或 Node 版本问题 | 换 `npmmirror` 源；确认 Node ≥ 18 |
| 前端改了没效果 | 浏览器缓存，或改错了文件 | 强刷（Ctrl+F5）；确认 vite dev server 没崩 |
| `git push` 被拒绝 | 没权限，或分支保护禁止直推 | 走 PR 流程；`main`/`dev` 禁止直接 push |
| 提示密钥泄露风险 | 提交了含 Key 的文件 | **立即轮换 Key**，并 `git rm --cached` 移除文件后提交 |

---

## 四、 给 AI Agent 的指令（零基础专用模板）

当你完全不知道该让 AI 做什么时，用这段：

```text
请阅读 GLOSSARY.md 和我的成员指南。
我没有任何编程经验，请你用大白话（不要甩专业术语）告诉我：

1. 我现在要做的事情，用一句话说是什么？
2. 第一步该做什么？请给我一条可以复制粘贴直接执行的命令。
3. 执行完我应该看到什么？如果看到的不是这个，可能是什么原因？
4. 做完后怎么验证成功？

在我确认"成功了"之前，不要开始下一步。
```
