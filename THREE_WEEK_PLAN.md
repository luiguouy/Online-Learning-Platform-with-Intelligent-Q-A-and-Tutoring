# 三周冲刺计划：每人每周任务与验收标准（v1.0 · 工期权威文件）

> **本文件是工期的唯一权威**。`TEAM_WORK_DIVISION.md` 第四章的**原 5 周路线图已作废**，仅保留其分工（第一、二章）与接口矩阵（第三章）。
> **总工期**：21 天（含周末）。**有效开发工时估算**：每人每周约 15 小时（上课日 2h × 5 + 周末 5h），三人周合计约 60 小时/人。
> **核心策略**：**并行开发 + Mock 解耦 + 每周一个硬门禁**。不做"后端写完前端再开始"的串行瀑布——3 周工期下瀑布必死。

---

## 一、 现实评估与裁剪原则（先读这节，再排任务）

### 1.1 必须先接受的三个现实

1. **零经验 + AI 生成代码 = 30% 时间在修编译错误**。任何"看起来 2 小时"的任务，实际按 1.5 倍估算。
2. **RAG 是全项目唯一的技术深水区**（向量化、切块、检索、流式），风险集中在成员 A 一人身上。**A 是单点故障，必须最先启动、最先验证**。
3. **3 周做不完原 5 周的全部内容**。必须有明确的裁剪清单（1.2），否则第 3 周会在"到处都是半成品"中崩盘。

### 1.2 需求优先级（MoSCoW）——时间不够时按此顺序砍

| 优先级 | 功能 | 负责 | 说明 |
| :--- | :--- | :--- | :--- |
| **Must 必做** | 登录鉴权（学生/教师角色） | B | 没有它一切都是空谈 |
| **Must** | 课件上传 → 切块 → 向量入库 | A+B | RAG 的地基 |
| **Must** | SSE 流式答疑 + 参考资料出处 | A+C | **答辩核心卖点，不能砍** |
| **Must** | 问答记录持久化与历史会话 | B+C | 教师纠偏的前提 |
| **Must** | 教师人工纠偏闭环 | B+D | **答辩核心卖点，不能砍** |
| **Must** | 课件解析状态展示 | B+D | 上传后能看到 PARSING→CHUNKED |
| **Should 应做** | 点赞/点踩反馈 | B+C | 成本低，有则更好 |
| **Should** | 学情看板（1~2 个图表） | B+D | 只做"每日提问量"+"课程分布"，不要贪多 |
| **Could 可做** | 知识点精解生成 | A+C | **时间紧第一个砍它** |
| **Could** | 自测题卡片 | C | 与知识点绑定，同上 |
| **Could** | 金标集 + recall@K 评测 | A+D | 有余力才做（第 3 周中段判断） |
| **Won't 不做** | MinIO 分布式存储 | B | 用本地绝对路径（规范已定） |
| **Won't** | 微服务 / Redis / 消息队列 | — | 规范明确禁止，AI Agent 可能自作主张加，见 README 铁律 |
| **Won't** | KaTeX 数学公式渲染 | C | `TEAM_WORK_DIVISION.md` 2.3 提过，但 C 指南未实现、依赖也未声明——**明确不做**，避免组员浪费时间 |

### 1.3 关键路径与死锁依赖（这张图决定了排期）

```text
Day1 环境就绪
  │
  ├─ B: 建表 + 登录接口 ──────────┐
  │                               ├─→ C/D 能登录，开始前端开发（Day 3 起）
  └─ B: 交付 3 个方法签名给 A ─────┤    ⚠ 死线 Day 3，否则 A 被完全阻塞
                                  │
  A: Chroma Spike（Day1~2）───────┼─→ A: 切块+检索（Day 4~8）
                                  │
                                  └─→ A: SSE 接口（Day 9~12）──→ C: 打字机界面对接（Day 10~14）
                                                                  │
  D: 上传界面（Mock，Day 3~6）────→ B: 真实上传接口（Day 7~9）──→ D: 对接真实接口（Day 10~12）
```

**最大风险点**：**B 必须在 Day 3 结束前把三个方法签名交给 A**（`saveStreamingRecord` / `findTopCorrected` / `createSessionLazy`，见 `MEMBER_B_DEV_GUIDE.md` 5.1）。哪怕内部先返回假数据，签名和返回类型必须冻结。**这是全项目唯一的硬死锁**，违反一次整体延期 3 天。

---

## 二、 第 1 周（Day 1~7）：地基与并行起步

**周目标**：环境跑通、契约冻结、四人各自能独立跑起来一块东西。

### 2.1 全员（Day 1，半天，组长牵头）

| # | 任务 | 验收标准 |
| :--- | :--- | :--- |
| 0.1 | 完成 `COLLABORATION_WORKFLOW.md` 附录 A 的**第 0 周准备清单 8 项** | 4 人都能 push 代码仓；测试 PR 能看到模板与 Actions 运行 |
| 0.2 | 建 GitHub Project 看板（To do / In progress / Done），把本周任务拆成 Issue 并指派 | 看板有卡片，每人有明确归属 |
| 0.3 | 各自本地环境自检：`java -v`（17）、`mvn -v`、`node -v`（≥18）、`docker ps` | 4 人全部截图发群 |

### 2.2 成员 A（组长 / RAG 核心）

| # | 任务 | 交付物 | 验收标准（可验证） | 截止 |
| :--- | :--- | :--- | :--- | :--- |
| A1.1 | **Chroma 元数据过滤 Spike**（最高优先级） | 一个最小 Demo + 结论 | 用 `IsEqualTo("courseId","1")` 检索**只返回该课程的结果**；`removeAll(filter)` 能删干净。**不通过立即启动退路方案**并通知全员 | Day 2 |
| A1.2 | 大模型连通性验证 | 一次成功的流式调用输出 | 用 `AGENT_INSTRUCTIONS.md` 1.2 的 yml 模板，跑通一次流式输出并截图 | Day 2 |
| A1.3 | Spring Boot 工程骨架 | 可启动的空工程 | `mvn spring-boot:run` 启动成功，Knife4j 页面能打开 | Day 3 |
| A1.4 | 文档解析器 + 切块 | `DocumentIngestionService` | 传入一个 PDF，能切出 N 个 400 字段落并打印数量 | Day 5 |
| A1.5 | 向量化写入 Chroma | 同上 | 切块后 Chroma 中 collection 的 count 增加 | Day 6 |

> **A1.1 否决权**：若 Day 2 发现 Chroma filter 不可用，**当天**决定改用 LangChain4j 内置向量存储 + 本地文件持久化，并立即更新 `AGENT_INSTRUCTIONS.md` 与 `MEMBER_A_DEV_GUIDE.md` 的配置段落（这是风险 R1 的预案，不要拖）。

### 2.3 成员 B（后端业务 / DB）

| # | 任务 | 交付物 | 验收标准 | 截止 |
| :--- | :--- | :--- | :--- | :--- |
| B1.1 | MySQL 建表（6 张表） | `schema.sql` | 执行后 `show tables;` 有全部 6 张表 | Day 2 |
| B1.2 | `Result<T>` 统一封装 + 全局异常处理 | `Result.java` / `GlobalExceptionHandler` | 任意异常返回 `{code,msg,data}` 结构，不返回堆栈 | Day 2 |
| B1.3 | Sa-Token 集成 + **StpInterfaceImpl** | `SaTokenConfigure` / `StpInterfaceImpl` | `teacher01/123456` 登录返回 token；**用教师 token 调 `/api/teacher/**` 不报 403** | Day 3 |
| B1.4 | 登录接口 `/api/auth/login` | `AuthController` | Postman 登录成功返回 token 与 role | Day 3 |
| B1.5 | **交付 3 个方法签名给 A**（硬死线） | 接口定义（可先返回假数据） | A 能直接调用 `saveStreamingRecord` / `findTopCorrected` / `createSessionLazy` 并编译通过 | **Day 3** |
| B1.6 | 课程列表接口 `/api/course/list` | `CourseController` | 返回种子课程数据 | Day 4 |
| B1.7 | Postman Mock 集合（给 C/D） | Mock 集合链接 | C/D 照 Mock 能完成登录与课程列表页面 | Day 4 |
| B1.8 | 种子数据 `data.sql` | SQL 文件 | 含 teacher01/student01，密码 BCrypt 加密，**实际登录验证通过** | Day 5 |

### 2.4 成员 C（学生端前端）

| # | 任务 | 交付物 | 验收标准 | 截止 |
| :--- | :--- | :--- | :--- | :--- |
| C1.1 | Vue3 + Vite + Element Plus 脚手架 | 可运行的空项目 | `npm run dev` 打开有页面 | Day 2 |
| C1.2 | 请求层 `request.ts`（含 token 拦截器） | TS 文件 | 请求自动带 `Authorization: Bearer <token>`；401 跳登录 | Day 3 |
| C1.3 | `sseClient.ts` 骨架（先按契约写，暂不联调） | TS 文件 | 能通过 `event:` 区分 4 类事件并打印到 console | Day 4 |
| C1.4 | 登录页 + 路由守卫 | Vue 页面 | 用种子账号能登录并跳转；未登录访问被拦截 | Day 5 |
| C1.5 | 主页布局（课程切换 + 侧边会话栏） | Vue 页面 | 页面结构完整，数据用 Mock | Day 7 |

### 2.5 成员 D（教师端前端）

| # | 任务 | 交付物 | 验收标准 | 截止 |
| :--- | :--- | :--- | :--- | :--- |
| D1.1 | 后台脚手架（复用 C 的技术栈） | 可运行项目 | `npm run dev` 能打开 | Day 2 |
| D1.2 | 后台布局 + 路由 + 角色守卫 | Vue 页面 | 学生账号访问后台被拒 | Day 4 |
| D1.3 | 课程管理页面（表格 + 新增） | Vue 页面 | 用 Mock 数据能渲染表格 | Day 5 |
| D1.4 | 课件上传界面（拖拽 + 进度条，先 Mock） | Vue 页面 | 能选择文件并显示上传进度（不真正上传） | Day 7 |
| D1.5 | 准备 3~5 篇测试课件 PDF | 文件 | 放入共享盘，全员可取 | Day 2 |

### 2.6 🚦 Gate 1（Day 7 晚，周末集成，组长主持）

**准入标准（全部满足才算过）**：

- [ ] 4 人的代码都能在 `dev` 分支上编译通过，CI 全绿
- [ ] 登录链路打通：前端 → 后端 → 数据库 → 返回 token
- [ ] B 的 3 个方法签名已交付，A 确认可调用
- [ ] Chroma Spike 有明确结论（通过 / 已切退路方案）
- [ ] **接口契约冻结**：`DEV_SPECIFICATION.md` 4.2 与接口矩阵不再改动（后续变更走四步法）
- [ ] 看板上本周任务全部 Done 或已明确延期原因

**未过 Gate 1 的处理**：组长当天组织 1 小时语音会，砍掉 Could 级需求，重新分配。

---

## 三、 第 2 周（Day 8~14）：核心闭环攻坚

**周目标**：**主链路打通**——上传课件 → 切块入库 → 学生提问 → 流式出答案 + 出处 → 教师纠偏。这是整个项目的生死线。

### 3.1 成员 A

| # | 任务 | 交付物 | 验收标准 | 截止 |
| :--- | :--- | :--- | :--- | :--- |
| A2.1 | Top-K 向量检索（含 courseId 隔离 + 阈值） | `RagRetrievalService` | 提问返回 Top-4 片段；**换 courseId 不会串课** | Day 9 |
| A2.2 | 防幻觉 Prompt 模板 | `PromptConstants` | 课件外的问题，模型回答"课件中未找到"而非编造 | Day 9 |
| A2.3 | **SSE 流式接口** `/api/qa/chat/stream` | `SseStreamService` | 用 curl 或 Knife4j 能看到 4 类事件依次输出，`done` 含 `recordId` | Day 11 |
| A2.4 | 会话懒创建（`sessionId=0`） | 同上 | 传 0 时自动建会话，`done` 回传真实 `sessionId` | Day 12 |
| A2.5 | **纠偏优先双路检索** | 同上 | 教师纠偏后，同样问题返回**新答案**并标注"权威修正" | Day 13 |
| A2.6 | 课件删除级联删向量 | `removeDocumentVectors` | 删课件后检索不到它的片段 | Day 14 |

### 3.2 成员 B

| # | 任务 | 交付物 | 验收标准 | 截止 |
| :--- | :--- | :--- | :--- | :--- |
| B2.1 | 课件上传接口 + 状态机 | `TeacherDocumentController` | 上传后状态 `PARSING` → 切块完成变 `CHUNKED`；**绝对路径存储，重启不丢** | Day 9 |
| B2.2 | 文件名与扩展名校验 | 同上 | 上传 `.exe` 被拒绝；文件名含 `../` 被拒绝 | Day 9 |
| B2.3 | 问答记录持久化 | `QaRecordService` | 一次问答后 `qa_record` 表有一条记录含 `grounding_references` | Day 10 |
| B2.4 | 会话列表 + 历史记录接口 | `QaSessionController` | 前端能拉取历史会话与对话内容 | Day 11 |
| B2.5 | 教师纠偏接口 `/api/teacher/qa/correct` | `TeacherQaController` | 提交纠偏后 `is_corrected=1` 且答案更新 | Day 12 |
| B2.6 | 点赞/点踩反馈接口 | 同上 | 提交后 `status` 字段更新（Should 级，可顺延） | Day 13 |
| B2.7 | 学情统计接口（2 个） | 统计接口 | 返回每日提问量与课程分布（Should 级） | Day 14 |

### 3.3 成员 C

| # | 任务 | 交付物 | 验收标准 | 截止 |
| :--- | :--- | :--- | :--- | :--- |
| C2.1 | **打字机流式渲染**（含节流） | `ChatWorkspace.vue` | 逐字输出不卡顿；**连续输出 500 字不掉帧** | Day 11 |
| C2.2 | Markdown + 代码高亮渲染 | `MarkdownViewer.vue` | 代码块有高亮；XSS 攻击字符串被过滤 | Day 12 |
| C2.3 | **参考资料出处抽屉** | `GroundingDrawer.vue` | 点"参考资料（3 处）"能展开课件名、分块号、相关度 | Day 13 |
| C2.4 | 对接真实 SSE（替换 Mock） | 联调 | 真实提问能流式出答案并展示出处 | Day 12 |
| C2.5 | 历史会话切换 | Vue 页面 | 切换会话能看到历史问答 | Day 14 |
| C2.6 | 加载态 / 空状态 / 错误态 | 全局 | 断网时显示友好错误而非白屏 | Day 14 |

### 3.4 成员 D

| # | 任务 | 交付物 | 验收标准 | 截止 |
| :--- | :--- | :--- | :--- | :--- |
| D2.1 | 对接真实上传接口 | 联调 | 上传真实 PDF 后后端能收到并触发切块 | Day 10 |
| D2.2 | 解析状态轮询展示 | 页面 | 上传后状态从 PARSING 变 CHUNKED（**3 秒轮询**） | Day 11 |
| D2.3 | 问答记录审计表格 | 页面 | 能按课程/时间筛选问答记录 | Day 12 |
| D2.4 | **人工纠偏弹窗** | `CorrectionDialog.vue` | 修改答案保存后，**学生端再问同样问题答案已变** | Day 13 |
| D2.5 | 课件删除（含级联） | 页面 | 删除后列表与检索都查不到 | Day 14 |

### 3.5 🚦 Gate 2（Day 14 晚）—— 全项目生死线

**准入标准**：**主链路必须端到端跑通**，在所有人面前演示一遍：

- [ ] 教师上传 PDF → 状态变 CHUNKED
- [ ] 学生提问 → 流式出答案 → 能展开参考资料出处
- [ ] 教师纠偏 → 学生再问 → 答案已更新
- [ ] 换一门课提问不会串答案
- [ ] 删除课件后不再被检索到
- [ ] `dev` 分支 CI 全绿，前后端无 P0/P1 缺陷

**未过 Gate 2**：立即砍掉全部 Could 级需求，全员转 Bug 修复，第 3 周只保主链路。

---

## 四、 第 3 周（Day 15~21）：收尾、验收与答辩

**周目标**：主链路打磨 + 演示零翻车 + 交付物封板。**Day 19 晚为代码冻结线**，之后只修 Bug 不改功能。

### 4.1 成员 A

| # | 任务 | 验收标准 | 截止 |
| :--- | :--- | :--- | :--- |
| A3.1 | 按用户限流（Guava RateLimiter） | 连续快速提问第 11 次返回 429 | Day 16 |
| A3.2 | 演示兜底：LLM 失败时回放历史答案 | 断网时返回缓存答案并标注，不白屏 | Day 17 |
| A3.3 | 金标集 + recall@K 评测（Could 级，有余力才做） | 跑出 Recall@3 数值 | Day 19 |
| A3.4 | 架构图与技术选型说明（答辩用） | 一张架构图 + 选型理由 | Day 19 |

### 4.2 成员 B

| # | 任务 | 验收标准 | 截止 |
| :--- | :--- | :--- | :--- |
| B3.1 | 边界与异常测试（空输入/超长/超大文件） | 均返回友好错误，不 500 | Day 16 |
| B3.2 | 修 P0/P1 缺陷 | 缺陷清单清零 | Day 18 |
| B3.3 | 种子数据复核 + 数据库脚本封板 | 一键建库建表可复现 | Day 19 |

### 4.3 成员 C

| # | 任务 | 验收标准 | 截止 |
| :--- | :--- | :--- | :--- |
| C3.1 | UI 打磨（骨架屏、空状态、移动端适配） | 无明显样式错位 | Day 17 |
| C3.2 | 修前端 P0/P1 缺陷 | 控制台无报错 | Day 18 |
| C3.3 | 演示数据准备（预置几个好问题） | 现场提问必有好效果 | Day 19 |

### 4.4 成员 D

| # | 任务 | 验收标准 | 截止 |
| :--- | :--- | :--- | :--- |
| D3.1 | ECharts 学情看板（1~2 个图表即可） | 图表有真实数据 | Day 17 |
| D3.2 | **全链路端到端测试**，出缺陷清单 | 覆盖主流程 + 边界用例 | Day 18 |
| D3.3 | **演示录屏**（3~5 分钟，兜底用） | 文件存本地，不依赖网络 | Day 19 |
| D3.4 | 软件工程文档 + 答辩 PPT | 文档齐全、PPT 有架构图与数据 | Day 20 |
| D3.5 | 组织 2 次模拟演练 | 全程 5 分钟内不翻车 | Day 20 |

### 4.5 🚦 Gate 3（Day 19 晚，代码冻结）

- [ ] P0/P1 缺陷清零，P2 有明确处理决定
- [ ] 演示录屏已完成并本地备份
- [ ] 演示前 30 分钟检查清单（`EVALUATION_AND_DEMO.md` 3.2）已演练一遍
- [ ] PPT 与文档封板
- [ ] `main` 分支已合并可演示版本

---

## 五、 每周汇报与门禁节奏

| 时间 | 事项 | 产出 |
| :--- | :--- | :--- |
| 每日 | 异步站会（群内 3 行，见 `COLLABORATION_WORKFLOW.md` 8.2） | 进度可见 |
| 每周三 21:00 | 中期集成：`feature` → `dev`，跑一次全链路 | 提前暴露冲突 |
| 每周日 21:00 | **Gate 评审 + 周报** | 过/不过的明确结论 |
| Day 19 | 代码冻结 | 只修 Bug |

**阻塞规则**：阻塞超 4 小时群内 @ 组长；超 8 小时开 15 分钟语音会。**零经验下卡 8 小时 = 方向错了，不要硬扛。**

---

## 六、 时间不够时的裁剪顺序（倒序砍）

当某一周明显完不成时，**按此顺序从下往上砍**，保证主链路完整：

1. 知识点精解生成 + 自测题卡片（Could）
2. 金标集与 recall@K 评测（Could）
3. 学情看板的第二个图表（保留"每日提问量"）
4. 点赞/点踩（Should）
5. 历史会话切换（保留记录持久化即可）

**任何情况下不得砍**：登录鉴权、课件上传切块、SSE 流式答疑 + 出处、教师纠偏闭环。这四项是答辩的全部卖点，砍掉等于"套壳 ChatGPT"。

---

## 七、 给 AI Agent 的周任务执行模板

组员每周开始时，把这段连同 `THREE_WEEK_PLAN.md` 一起喂给自己的 Agent：

```text
请阅读 THREE_WEEK_PLAN.md 中我（成员 X）本周的任务表，以及我的 MEMBER_X_DEV_GUIDE.md。

本周我的任务是：<抄写任务编号与标题，如 A2.1 Top-K 向量检索>

执行要求：
1. 先复述一遍验收标准，确认你理解"怎样算做完"。
2. 列出要创建/修改的文件清单，我确认后再动手。
3. 一次只做一个任务（不要顺手做下一个）。
4. 完成后，用验收标准里的可验证条件逐条自测，把结果贴给我
   （例如"检索返回 4 条，换 courseId=2 后结果不同"——要具体证据，不要只说"已完成"）。
5. 编译必须通过；报错要说明根因再改，不允许"换个写法试试"。
```
