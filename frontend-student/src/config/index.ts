/**
 * 全局开关
 *
 * Week 1（C1.1 ~ C1.5）阶段：成员 B 的登录接口（B1.4，Day 3）与课程列表接口（B1.6，Day 4）
 * 尚在推进，A 的 SSE 接口（A2.3，Day 11）未到联调期，前端按文档要求「先 Mock」开发，
 * 以便 C1.4 登录跳转、C1.5 主页布局能独立自测。
 *
 * 对接真实接口时：把 USE_MOCK 改为 false，并删除 src/mock/ 整个目录
 * （清理清单见 README.md 第七节）。严禁在交付版本中保留 USE_MOCK = true。
 */
export const USE_MOCK = true;
